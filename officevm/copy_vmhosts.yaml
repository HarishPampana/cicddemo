- name: Ansible Copy hosts file Local to Remote VM's
  hosts: 'kmasters,kworkers'
  tasks:
    - name: copying file with playbook
      become: true 
      copy:
        src: ~/cicddemo/officevm/config_file/vm_hosts
        dest: /etc/hosts
        owner: root
        group: root        
        mode: 0644
